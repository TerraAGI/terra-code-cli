{
  "name": "terra-code-vscode-ide-companion",
  "displayName": "Terra Code Companion",
  "description": "Enable Terra Code with direct access to your VS Code workspace.",
  "version": "0.0.10",
  "publisher": "terra-code",
  "icon": "assets/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/terra-code/terra-code.git",
    "directory": "packages/vscode-ide-companion"
  },
  "engines": {
    "vscode": "^1.99.0"
  },
  "license": "LICENSE",
  "preview": true,
  "categories": [
    "AI"
  ],
  "keywords": [
    "terra-code",
    "terra code",
    "terra",
    "terra code",
    "cli",
    "ide integration",
    "ide companion"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "languages": [
      {
        "id": "terra-diff-editable"
      }
    ],
    "commands": [
      {
        "command": "terra.diff.accept",
        "title": "Terra Code: Accept Current Diff",
        "icon": "$(check)"
      },
      {
        "command": "terra.diff.cancel",
        "title": "Terra Code: Close Diff Editor",
        "icon": "$(close)"
      },
      {
        "command": "terra-code.runTerraCode",
        "title": "Terra Code: Run"
      },
      {
        "command": "terra-code.showNotices",
        "title": "Terra Code: View Third-Party Notices"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "terra.diff.accept",
          "when": "terra.diff.isVisible"
        },
        {
          "command": "terra.diff.cancel",
          "when": "terra.diff.isVisible"
        }
      ],
      "editor/title": [
        {
          "command": "terra.diff.accept",
          "when": "terra.diff.isVisible",
          "group": "navigation"
        },
        {
          "command": "terra.diff.cancel",
          "when": "terra.diff.isVisible",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "terra.diff.accept",
        "key": "ctrl+s",
        "when": "terra.diff.isVisible"
      },
      {
        "command": "terra.diff.accept",
        "key": "cmd+s",
        "when": "terra.diff.isVisible"
      }
    ]
  },
  "main": "./dist/extension.cjs",
  "type": "module",
  "scripts": {
    "vscode:prepublish": "npm run generate:notices && npm run check-types && npm run lint && node esbuild.js --production",
    "build": "npm run compile",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "vsce package --no-dependencies",
    "generate:notices": "node ./scripts/generate-notices.js",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:ci": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "20.x",
    "@types/vscode": "^1.99.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "esbuild": "^0.25.3",
    "eslint": "^9.25.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^3.25.76"
  }
}
